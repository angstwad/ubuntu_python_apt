---
galaxy_info:
  author: Paul Durivage
  description: Idempotent installer for python-apt module (prevents "python-apt is not installed" errors)
  license: Apache v2.0
  min_ansible_version: 1.2
  platforms:
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all
  categories:
  - system
dependencies: []